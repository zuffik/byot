import * as React from 'react';
import {createPortal} from '../functional/Portals';

export const {
  PortalIn: PlainLayoutBackButtonPortalIn,
  PortalOut: PlainLayoutBackButtonPortalOut,
  PortalsProvider: PlainLayoutBackButtonPortalProvider,
} = createPortal();
