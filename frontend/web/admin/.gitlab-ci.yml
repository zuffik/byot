include: '/frontend/web/.gitlab-ci.yml'

before_script:
  - cd frontend/web/admin
  - yarn install

Build:
  artifacts:
    paths:
      - frontend/web/admin
  extends: .build

Test:
  dependencies:
    - Build
  extends: .test

Deploy:
  dependencies:
    - Build
    - Test
  extends: .deploy
