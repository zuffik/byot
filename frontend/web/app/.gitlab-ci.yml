include: '/frontend/web/.gitlab-ci.yml'

before_script:
  - cd frontend/web/app
  - yarn install

Build:
  artifacts:
    paths:
      - frontend/web/app
  extends: .build

Test:
  dependencies:
    - Build
  extends: .test

Deploy:
  dependencies:
    - Build
    - Test
  extends: .deploy
