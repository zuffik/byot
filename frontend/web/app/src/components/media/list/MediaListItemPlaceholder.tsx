import * as React from 'react';
import {TrainingSetListItemImagePlaceholder} from '../../training/set/TrainingSetListItemImagePlaceholder';

interface Props {}

export const MediaListItemPlaceholder: React.FC<Props> = (props: Props) => {
  return <TrainingSetListItemImagePlaceholder />;
};
