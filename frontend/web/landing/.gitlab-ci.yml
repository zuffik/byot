include: '/frontend/web/.gitlab-ci.yml'

before_script:
  - cd frontend/web/landing
  - yarn install

Build:
  artifacts:
    paths:
      - frontend/web/landing
  extends: .build

Test:
  extends: .test

Deploy:
  extends: .deploy
